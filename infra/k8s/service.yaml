apiVersion: v1
kind: Service
metadata:
  name: amrutam-backend
  namespace: amrutam
  labels:
    app.kubernetes.io/name: amrutam-backend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: amrutam-backend
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
